# Прямые зависимости — всё остальное подтянется само.
# socks — чтобы бот умел ходить через прокси;
# без этого дополнения запуск с TELEGRAM_PROXY падает
python-telegram-bot[socks]==22.8
python-dotenv==1.2.2

# Тесты
pytest==9.1.1

# pyflakes ловит то, чего не видят тесты: обращение к необъявленному имени
# внутри функции, которую тесты не вызывают. Именно так однажды уехал
# в бой NameError в main.py — со всеми зелёными проверками
pyflakes==3.4.0
